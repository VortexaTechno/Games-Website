/* ── Styles ── */
import "./style.css";

/* ── AOS (Animate On Scroll) ── */
import AOS from "aos";

/* ── Swiper ── */
import Swiper from "swiper";

/* ── Lucide Icons ── */
import { createIcons, icons } from "lucide";

/* ── Application Entry ── */
import "./js/main.js";

/* ── Initialize AOS ── */
AOS.init({
  duration: 800,
  once: true,
});
